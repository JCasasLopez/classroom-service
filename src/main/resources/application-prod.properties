spring.datasource.url=jdbc:mysql://localhost:3306/classrooms
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=Vindel39!
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.h2.console.enabled=false

jwt.secretKey=uN9W8p7rQ2v5y8x/A?D(G+KbPeShVmYq3t6w9z$C&F)J@NcQ
# jwt.secretKey=${JWT_SECRETKEY}

# Kafka Bootstrap Servers
spring.kafka.producer.bootstrap-servers=127.0.0.1:9092

# Serializers for Key and Value
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer

# Custom property for the topic name
spring.kafka.producer.topic-name=classrooms-list

# KEY SETTING: Disables sending the Java class type in the message headers.
# This prevents the "__TypeId__" header from being sent, allowing 
# the consumer to use its own local copy of the event class.
spring.kafka.producer.properties.spring.json.add.type.headers=false